<Application
    x:Class="PinguTools.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit">
    <Application.Resources>
        <ResourceDictionary>
            <ImageSource x:Key="ErrorIcon">pack://application:,,,/Resources/cross-circle.png</ImageSource>
            <ImageSource x:Key="WarningIcon">pack://application:,,,/Resources/exclamation.png</ImageSource>
            <ImageSource x:Key="InfoIcon">pack://application:,,,/Resources/information.png</ImageSource>

            <Style TargetType="Button" x:Key="ActionButton">
                <Setter Property="Width" Value="90" />
            </Style>

            <Style TargetType="TextBlock" x:Key="FloatText">
                <Setter Property="FontSize" Value="11" />
                <Setter Property="Foreground" Value="Gray" />
                <Setter Property="HorizontalAlignment" Value="Left" />
                <Setter Property="IsHitTestVisible" Value="False" />
                <Setter Property="Margin" Value="2" />
                <Setter Property="VerticalAlignment" Value="Bottom" />
                <Setter Property="Panel.ZIndex" Value="100" />
            </Style>

            <Style TargetType="StackPanel" x:Key="ActionPanelStyle">
                <Setter Property="Margin" Value="0,8,0,0" />
                <Setter Property="Orientation" Value="Horizontal" />
                <Setter Property="HorizontalAlignment" Value="Right" />
            </Style>

            <Style TargetType="xctk:DecimalUpDown">
                <Setter Property="TextAlignment" Value="Left" />
                <Setter Property="Increment" Value="0.1" />
            </Style>

            <Style TargetType="xctk:IntegerUpDown">
                <Setter Property="TextAlignment" Value="Left" />
            </Style>

            <Style TargetType="xctk:UIntegerUpDown">
                <Setter Property="TextAlignment" Value="Left" />
            </Style>

            <Style TargetType="xctk:DoubleUpDown">
                <Setter Property="TextAlignment" Value="Left" />
                <Setter Property="Increment" Value="0.1" />
            </Style>

            <Style TargetType="GroupBox">
                <Setter Property="Padding" Value="2" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>