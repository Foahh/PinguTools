<Window
    MaxWidth="500"
    ResizeMode="NoResize"
    SizeToContent="WidthAndHeight"
    d:DataContext="{d:DesignInstance Type=viewModels:MainWindowViewModel}"
    mc:Ignorable="d"
    x:Class="PinguTools.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:pages="clr-namespace:PinguTools.Pages"
    xmlns:viewModels="clr-namespace:PinguTools.ViewModels"
    xmlns:localization="clr-namespace:PinguTools.Localization"
    xmlns:converters="clr-namespace:PinguTools.Converters">

    <Window.Resources>
        <converters:BoolHandCursorConverter x:Key="BoolHandCursorConverter" />
    </Window.Resources>

    <DockPanel LastChildFill="True">
        <StatusBar DockPanel.Dock="Bottom" Padding="2, 0">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusTime, StringFormat='{}{0:HH:mm:ss}'}" />
            </StatusBarItem>
            <Separator Margin="2,0" />
            <StatusBarItem>
                <TextBlock Text="{Binding Status}" />
            </StatusBarItem>
            <Separator Margin="2,0" />
            <StatusBarItem>
                <TextBlock Text="{Binding UpdateStatus}"
                           Cursor="{Binding IsUpdateAvailable, Converter={StaticResource BoolHandCursorConverter}}"
                           ToolTip="{x:Static localization:Strings.UpdateCheck_Tooltip}">
                    <TextBlock.InputBindings>
                        <MouseBinding Command="{Binding DownloadUpdateCommand}" MouseAction="LeftClick" />
                    </TextBlock.InputBindings>
                </TextBlock>
            </StatusBarItem>
        </StatusBar>

        <TabControl Margin="8">
            <TabItem Header="{x:Static localization:Strings.Tab_Workflow}">
                <pages:WorkflowTab />
            </TabItem>
            <TabItem Header="{x:Static localization:Strings.Tab_Chart}">
                <pages:ChartTab />
            </TabItem>
            <TabItem Header="{x:Static localization:Strings.Tab_Jacket}">
                <pages:JacketTab />
            </TabItem>
            <TabItem Header="{x:Static localization:Strings.Tab_Music}">
                <pages:MusicTab />
            </TabItem>
            <TabItem Header="{x:Static localization:Strings.Tab_Stage}">
                <pages:StageTab />
            </TabItem>
            <TabItem Header="{x:Static localization:Strings.Tab_Misc}">
                <pages:MiscTab />
            </TabItem>

            <!-- TODO: Bulk Tab -->
        </TabControl>
    </DockPanel>
</Window>