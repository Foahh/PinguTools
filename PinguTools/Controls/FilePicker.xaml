<UserControl
    x:Class="PinguTools.Controls.FilePicker"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>

    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}}">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger
                                Binding="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                Value="">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                            <DataTrigger
                                Binding="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <TextBlock
                Foreground="Red"
                Text="*"
                VerticalAlignment="Top"
                Visibility="{Binding Required, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVisibilityConverter}}" />
        </StackPanel>

        <DockPanel>
            <Button
                Command="{Binding BrowseFileCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Content="..."
                DockPanel.Dock="Right"
                Width="30" />
            <TextBox
                DockPanel.Dock="Left"
                IsReadOnly="True"
                Margin="0,0,4,0"
                Text="{Binding Path, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}" />
        </DockPanel>
    </StackPanel>
</UserControl>